# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
dist/
dist-ssr/
*.local
coverage/

# Editor/agent tooling state — never part of the product
.claude/

# Secrets — these must never be committed.
# The real ones live outside the repository under ~/.local/share/lifeops,
# but a stray copy here would be a disclosure.
*.key
secrets.json
.env
.env.*
!.env.example
nornicdb.env

# LifeOps runtime state, if a state dir is ever pointed inside the repo
lifeops.config.json
nornicdb-data/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp
