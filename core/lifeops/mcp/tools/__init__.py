"""Tool implementations.

Phase 0's five tools are defined inline in ``lifeops.mcp.server`` because they
are thin wrappers over LifeOpsCore. This package exists for the larger tool
surface that arrives from Phase 2 onward (BUILD_SPEC sections 50 and 51).
"""
